// SPA mode: render entirely on the client. There is no Node server at
// runtime — the app deploys as static files behind an index.html fallback.
export const ssr = false;
export const prerender = false;
